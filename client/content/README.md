# content/ — bundled into the .wuhb (mounted at /vol/content)

Goes inside the `.wuhb` and is read-only at runtime. Assets:
- `cacert.pem` (TLS roots for libcurl when talking to Deezer over HTTPS)
- `font.ttf` (UI font for SDL2_ttf)
